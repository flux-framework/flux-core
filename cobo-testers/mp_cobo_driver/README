To run this COBO/CMB tester:

1) Get a SLURM allocation
	$ salloc -N 16

2) Start up an CMB session
	$ cd ../../zmq-broker 
	$ scripts/launch -c binary

3) At the a new shell the step 2 creates, 
   change back into the directory where
   this tester resides. This tester takes 
   one argument that specifies the number
   of COBO back-end processes. 

	$ mp_cobo_fen 256

